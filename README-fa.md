# پیکربندی Neovim علیرضا

<div align="center">

[![Neovim](https://img.shields.io/badge/NeoVim-%2357A143.svg?&style=for-the-badge&logo=neovim&logoColor=white)](https://neovim.io/)
[![Lua](https://img.shields.io/badge/Lua-2C2D72?style=for-the-badge&logo=lua&logoColor=white)](https://www.lua.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)](https://opensource.org/licenses/MIT)

</div>

پیکربندی Neovim بسیار سفارشی‌شده‌ای که بر اساس [AstroNvim](https://github.com/AstroNvim/AstroNvim) ساخته شده است، برای توسعه کارآمد طراحی شده و تمرکزی بر ابزارهای مدرن و تجربه کاربری مناسب دارد.

## ✨ ویژگی‌ها

- **مدیریت افزونه‌های مدرن**: استفاده از `lazy.nvim` برای مدیریت سریع و کارآمد افزونه‌ها
- **یکپارچه‌سازی LSP**: پشتیبانی کامل از پروتکل سرور زبان (LSP) با سرورهای از پیش پیکربندی شده
- **Tree-sitter**: برجسته‌سازی نحوی و تجزیه پیشرفته
- **Telescope**: یابنده فازی قدرتمند برای فایل‌ها، بافرها و بیشتر
- **یکپارچه‌سازی Git**: گردش کار Git یکپارچه با `gitsigns.nvim`
- **پشتیبانی اشکال‌زدایی**: پیکربندی DAP (پروتکل اشکال‌زدایی) داخلی
- **یکپارچه‌سازی هوش مصنوعی**: شامل افزونه‌هایی مانند Avante برای کدنویسی کمک‌شده از هوش مصنوعی
- **رابط کاربری زیبا**: خط وضعیت سفارشی، خط بافر و مرورگر فایل با نمادها
- **تکمیل هوشمند**: تکمیل پیشرفته و قطعه‌های کد هوشمند

## 📦 افزونه‌های موجود

این پیکربندی شامل مجموعه‌ای از افزونه‌های انتخاب شده با دقت برای تجربه توسعه بهینه است:

### افزونه‌های هسته
- `telescope.nvim` - یابنده فازی
- `nvim-treesitter` - برجسته‌سازی نحوی
- `lsp-zero` - پیکربندی LSP
- `mason.nvim` - مدیر سرور زبان/اشکال‌زدایی/تعمیرکار/فرمت‌دهنده
- `nvim-web-devicons` - نمادهای نوع فایل
- `which-key.nvim` - نکات مربوط به کلیدهای میانبر
- `nvim-autopairs` - بستن خودکار جفت‌ها
- `comment.nvim` - تغییر وضعیت هوشمند توضیحات

### بهبود رابط کاربری
- `alpha-nvim` - داشبورد
- `lualine.nvim` - خط وضعیت
- `bufferline.nvim` - خط بافر/تب
- `neo-tree.nvim` - مرورگر فایل
- `toggleterm.nvim` - ترمینال قابل تغییر وضعیت
- `trouble.nvim` - لیست تشخیص‌های LSP
- `snacks.nvim` - بهبودهای اضافی رابط کاربری

### ابزارهای توسعه
- `nvim-dap` - پروتکل اشکال‌زدایی
- `nvim-dap-go` - اشکال‌زدایی زبان Go
- `telescope` افزونه‌هایی برای ابزارهای مختلف
- `goto-preview` - پیش‌نمایش تعاریف در پنجره‌های شناور
- `actions-preview` - پیش‌نمایش اقدامات
- `luasnip` - موتور قطعه‌کد

### هوش مصنوعی و کمک در کدنویسی
- `avante.nvim` - دستیار هوش مصنوعی
- `codecompanion.nvim` - دستیار کدنویسی هوش مصنوعی
- `blink.cmp` - موتور تکمیل مدرن

## ⚡ نیازمندی‌ها

- [Nerd Fonts](https://www.nerdfonts.com/font-downloads) (_لازم برای نمادها_)
- [Neovim 0.9.5+ (_نه_ نسخه nightly)](https://github.com/neovim/neovim/releases/tag/stable)
- [CLI Tree-sitter](https://github.com/tree-sitter/tree-sitter/blob/master/cli/README.md) (_توجه:_ فقط در صورتی که بخواهید از ویژگی `auto_install` با Treesitter استفاده کنید، ضروری است)
- ابزار کلیپ‌بورد برای یکپارچه‌سازی با کلیپ‌بورد سیستم (برای راه‌حل‌های پشتیبانی شده به [`:help clipboard-tool`](https://neovim.io/doc/user/provider.html#clipboard-tool) مراجعه کنید)
- ترمینال با پشتیبانی از رنگ واقعی (برای پوسته پیش‌فرض، در غیر این صورت به پوسته‌ای که استفاده می‌کنید بستگی دارد)

### نیازمندی‌های اختیاری
- [ripgrep](https://github.com/BurntSushi/ripgrep) - جستجوی grep زنده در telescope (`<leader>fw`)
- [lazygit](https://github.com/jesseduffield/lazygit) - واسط گرافیکی Git در ترمینال قابل تغییر (`<leader>tl` یا `<leader>gg`)
- [gdu](https://github.com/dundee/gdu) - نمایش میزان استفاده از دیسک در ترمینال قابل تغییر (`<leader>tu`)
- [bottom](https://github.com/ClementTsang/bottom) - نمایش فرآیندها در ترمینال قابل تغییر (`<leader>tt`)
- [Python](https://www.python.org/) - ترمینال REPL پایتون قابل تغییر (`<leader>tp`)
- [Node](https://nodejs.org/en/) - ترمینال REPL نود قابل تغییر (`<leader>tn`)

> [!NOTE]
> <sup id="1">[1]</sup> تمام Nerd Fonts قابل دانلود شامل نمادهایی هستند که توسط این پیکربندی استفاده می‌شوند. Nerd Font مورد نظر خود را روی سیستم خود نصب کنید و در تنظیمات ترمینال خود، صورت فونت خود را روی آن Nerd Font تنظیم کنید. اگر از این پیکربندی در یک سیستم راه دور از طریق SSH استفاده می‌کنید، نیازی به نصب فونت روی سیستم راه دور نیست.

> [!NOTE]
> <sup id="2">[2]</sup> توجه در هنگام استفاده از پوسته پیش‌فرض: برای مک‌اواس، ترمینال پیش‌فرض از رنگ واقعی پشتیبانی نمی‌کند. باید از [iTerm2](https://iterm2.com/)، [Kitty](https://sw.kovidgoyal.net/kitty/)، [WezTerm](https://wezfurlong.org/wezterm/) یا [ترمینال ایمیتاتور](https://github.com/termstandard/colors?tab=readme-ov-file#truecolor-support-in-output-devices) دیگری که از رنگ واقعی پشتیبانی می‌کند استفاده کنید.

## 🛠️ نصب

### لینوکس/مک اواس (یونیکس)

#### پشتیبان‌گیری از پوشه nvim و اشتراک فعلی شما

```shell
mv ~/.config/nvim ~/.config/nvim.bak
mv ~/.local/share/nvim ~/.local/share/nvim.bak
mv ~/.local/state/nvim ~/.local/state/nvim.bak
mv ~/.cache/nvim ~/.cache/nvim.bak
```

#### کلون کردن مخزن

```shell
git clone --depth 1 https://github.com/alirezamoosavi/nvim-config ~/.config/nvim
rm -rf ~/.config/nvim/.git
nvim
```

## 🚀 استفاده

پس از نصب، Neovim را با دستور `nvim` راه‌اندازی کنید. پیکربندی به طور خودکار تمام افزونه‌ها را در اولین اجرا نصب می‌کند. این ممکن است چند دقیقه طول بکشد بسته به اتصال اینترنت و عملکرد سیستم شما.

### تغییر مکان کلیدها

این پیکربندی از سیستم تغییر مکان کلید مبتنی بر لیدر با `<leader>` (فاصله) به عنوان کلید اصلی استفاده می‌کند:

- `<leader>ff` - یافتن فایل‌ها با telescope
- `<leader>fg` - جستجوی grep زنده با telescope
- `<leader>fb` - یافتن بافرها با telescope
- `<leader>fh` - یافتن راهنما با telescope
- `<leader>gg` - باز کردن lazygit
- `<leader>ll` - باز کردن لاگ LSP
- `<leader>li` - نمایش اطلاعات LSP
- `<leader>rn` - تغییر نام نماد زیر مکان‌نما
- `<leader>ca` - عمل کد
- `<leader>gd` - رفتن به تعریف
- `<leader>gr` - رفتن به مراجع
- `<leader>q` - خروج
- `<leader>w` - ذخیره
- `<leader>nt` - تغییر وضعیت درخت فایل
- `<leader>tt` - تغییر وضعیت ترمینال
- `<leader>tf` - تغییر وضعیت درخت فایل و ترمینال به صورت عمودی

برای مشاهده کامل تغییر مکان کلیدها، از `<leader>kh>` برای باز کردن منوی which-key استفاده کنید.

## 🎨 سفارشی‌سازی

برای سفارشی کردن این پیکربندی:

1. این مخزن را فورک کنید
2. فایل‌های Lua موجود در دایرکتوری `lua/alireza/` را تغییر دهید:
   - `core/` شامل پیکربندی عمومی (گزینه‌ها، تغییر مکان کلیدها، autocmds)
   - `plugins/` شامل پیکربندی‌های مخصوص افزونه
   - `lazy.lua` مدیریت بارگذاری افزونه‌ها
3. تغییرات خود را ثبت و ارسال کنید

## 🤝 مشارکت

لطفاً این مخزن را فورک کنید و درخواست‌های ادغام ارسال کنید. همچنین می‌توانید برای اشکالات یا درخواست ویژگی‌ها، مسائلی ایجاد کنید.

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است - برای جزئیات بیشتر فایل [LICENSE](LICENSE) را مشاهده کنید.

## 👤 نویسنده

**علیرضا موسوی**

- گیت‌هاب: [@alirezamoosavi](https://github.com/alirezamoosavi)
- مخزن: [nvim-config](https://github.com/alirezamoosavi/nvim-config)

## 🙏 قدردانی

- [AstroNvim](https://github.com/AstroNvim/AstroNvim) برای پایه عالی
- تمام نویسندگان افزونه‌هایی که کارشان این پیکربندی را ممکن می‌سازد
- جامعه Neovim برای بهبودهای مداوم و پشتیبانی